{
  "name": "discord-global-mod-bot",
  "version": "1.0.0",
  "description": "Discord moderation bot with cross-server global bans by Discord user ID.",
  "type": "module",
  "main": "src/index.js",
  "scripts": {
    "start": "node src/index.js",
    "deploy": "node src/deploy-commands.js",
    "check": "node --check src/index.js && node --check src/config.js && node --check src/store.js && node --check src/bans.js && node --check src/commands.js && node --check src/deploy-commands.js"
  },
  "keywords": [
    "discord",
    "moderation",
    "bot",
    "global-ban"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "discord.js": "^14.25.1",
    "dotenv": "^17.3.1"
  },
  "overrides": {
    "undici": "^6.24.1"
  }
}
